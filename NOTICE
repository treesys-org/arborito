Third-party notices — Arborito (this directory)

Trademarks and unofficial builds (project policy — not legal advice): see docs/forking-and-branding.md

Gun (vendored browser scripts in vendor/gun/, loaded same-origin — no Gun script CDN)
  SPDX-License-Identifier: (Zlib OR MIT OR Apache-2.0) — see upstream https://github.com/amark/gun
  Source note: vendor/gun/SOURCE.txt

Tailwind CSS (build-time dependency; compiled CSS is bundled from this origin — no CDN)
  SPDX-License-Identifier: MIT
  Copyright (c) Tailwind Labs, Inc.
  Full license text: https://github.com/tailwindlabs/tailwindcss/blob/master/LICENSE

When you run `npm install` in this folder, npm records exact versions in package-lock.json. Other packages pulled in by the Tailwind toolchain (for example PostCSS, Autoprefixer) are subject to their respective SPDX licenses; run `npm ls --all` and consult each package’s LICENSE file for audit trails.

Bundled CSS output
  The file `src/styles/main.css` is generated by the build. It combines (a) your own source styles imported from `main.entry.css`—including `foundation/tokens.css` and other project CSS, under **GPL-3.0** like the rest of Arborito—with (b) utility rules emitted by the Tailwind compiler, which are covered by **Tailwind’s MIT license** as third-party output. This NOTICE is about (b) and the Tailwind npm packages; your application source files remain under the license in `LICENSE`.
