/node_modules
/dist
/release
/package
/www
/android
*.cache
.cache/
.chromium-tmp/
*.tgz

# Editor / IDE local (never publish — rules live in ../confidencial/cursor/)
.cursor/
.cursorignore
brainstorming.txt
knip.json

# CSS: fuente en src/shared/styles/** (main.entry.css + parciales shared) y src/features/*/styles/** (CSS por feature). Salida compilada main.css
# debe commitearse para GitHub Pages si el deploy es desde rama (no solo Actions).
# Regenerar: npm run build:css  (o build:css:min para producción)

# Entorno y secretos (nunca commitear)
.env
.env.*
!.env.example
*.jks
*.keystore
github-secrets.env
**/arborito-release.jks

# Sistema / logs
.DS_Store
Thumbs.db
*.log
logsaca
reports/

# Pruebas / cobertura
coverage/
.nyc_output/

# Python (scripts/validate-locales.py et al.)
__pycache__/
*.pyc

# Electron-builder / empaquetado local (por si quedan fuera de /dist)
builder-debug.yml
builder-effective-config.yaml
*.blockmap
linux-unpacked/
win-unpacked/
__flatpak-x86_64/

# Capacitor intermedios
.capacitor/

# Local course dumps (never publish)
*.arborito

# npm / linters
.npm/
.eslintcache
*.tsbuildinfo

# GitSync local (reglas viven junto a gitsync.py o en ignorar.txt del repo padre)
/ignorar.txt
/gitsync.py
repos_backups/
_gitsync_tmp/
confidencial/
.auth-writer.json
