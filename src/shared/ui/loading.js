/** Loading ring size tiers — used by `Loading.jsx` (React). Boot splash reference: 52px. */
export const LOADING_RING_BOOT = 52;

export const LOADING_RING_SIZE = Object.freeze({
    xs: 20,
    sm: 28,
    md: 36,
    lg: 44,
    boot: LOADING_RING_BOOT,
});
